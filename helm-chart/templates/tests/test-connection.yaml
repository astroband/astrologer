apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "astrologer.fullname" . }}-test-connection"
  labels:
{{ include "astrologer.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "astrologer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
